# Local stack credentials for infra/docker-compose.yml (CLAUDE.md §6.10 —
# secrets never enter the repo; this file documents shape only).
# Run with: docker compose -f infra/docker-compose.yml --env-file infra/.env up
POSTGRES_USER=daycare
POSTGRES_PASSWORD=daycare
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest
