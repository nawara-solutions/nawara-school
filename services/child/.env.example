PORT=3004
DATABASE_URL=postgresql://daycare:daycare@localhost:5432/child_db
RABBITMQ_URL=amqp://guest:guest@localhost:5672
JWT_PUBLIC_KEY=

# Separate key for the restricted medical/allergy column set, encrypted at
# rest and never shared with another service's key (CLAUDE.md §6.4).
MEDICAL_DATA_ENCRYPTION_KEY=
