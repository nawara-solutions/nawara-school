# Build-time config only — no secrets belong here (tokens live in
# flutter_secure_storage, never bundled — CLAUDE.md §10).
# Run with: flutter run --dart-define-from-file=.env
API_BASE_URL=http://localhost:3000
