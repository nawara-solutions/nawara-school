# React frontend config, read at build time by Vite. No secrets: the
# desktop app talks to the gateway over HTTPS and never holds a service key
# (privileged work is the Rust side's job — CLAUDE.md §10).
VITE_API_BASE_URL=http://localhost:3000
